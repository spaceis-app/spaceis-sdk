{
  "name": "spaceis-sdk",
  "private": true,
  "description": "SpaceIS SDK monorepo — JavaScript SDK + React bindings",
  "scripts": {
    "build": "pnpm --filter @spaceis/sdk build && pnpm --filter @spaceis/react build && pnpm --filter spaceis-react-example build",
    "dev": "pnpm -r --parallel dev",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "clean": "pnpm -r exec rm -rf dist node_modules"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "packageManager": "pnpm@9.15.0"
}
